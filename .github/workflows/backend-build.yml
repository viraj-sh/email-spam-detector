name: Executable CI

on:
  push:
    branches:
      - "**"
    tags:
      - "v*.*.*"

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: windows-latest
            spec: .github/packaging/pyinstaller/win_x64.spec
            ext: win-x64.exe
          - os: macos-14
            spec: .github/packaging/pyinstaller/mac_arm64.spec
            ext: macos-arm64
          - os: ubuntu-24.04
            spec: .github/packaging/pyinstaller/linux_x86_64.spec
            ext: linux-x86_64

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements/base.txt
          pip install pyinstaller

      - name: Build executable
        shell: bash
        run: |
          echo "Building for OS: ${{ matrix.os }}"
          pyinstaller "${{ matrix.spec }}" --noconfirm --clean
          mkdir -p build-output

          # Get version from tag or commit
          if [[ "${GITHUB_REF}" == refs/tags/* ]]; then
            version="${GITHUB_REF#refs/tags/}"
          else
            version="commit-$(git rev-parse --short HEAD)"
          fi

          exe_name="email-spam-detector-${version}-${{ matrix.ext }}"

          # Copy built binary to output folder
          if [[ "${{ matrix.os }}" == "windows-latest" ]]; then
            cp "dist/email-spam-detector.exe" "build-output/${exe_name}"
          else
            cp "dist/email-spam-detector" "build-output/${exe_name}"
          fi

          echo "EXE_NAME=${exe_name}" >> $GITHUB_ENV

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.EXE_NAME }}
          path: build-output/${{ env.EXE_NAME }}

  test-windows:
    name: Test Windows
    needs: build
    runs-on: windows-latest

    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          path: ./artifacts

      - name: Locate Windows binary
        id: find
        shell: pwsh
        run: |
          $file = Get-ChildItem -Path ./artifacts -Recurse -File -Filter "email-spam-detector-*-win-x64.exe" | Select-Object -First 1
          if (-Not $file) { Write-Error "Windows binary not found" }
          echo "exe_path=$($file.FullName)" | Out-File -FilePath $env:GITHUB_OUTPUT -Append

      - name: Start server
        shell: pwsh
        run: |
          Start-Process -FilePath "${{ steps.find.outputs.exe_path }}" -ArgumentList "--port 8000" -WindowStyle Hidden
          Start-Sleep -Seconds 30

      - name: Test /api/v1/health
        shell: pwsh
        run: |
          $maxRetries = 10
          $success = $false

          for ($i = 0; $i -lt $maxRetries; $i++) {
              try {
                  $response = Invoke-WebRequest -Uri "http://127.0.0.1:8000/api/v1/health" -UseBasicParsing -TimeoutSec 5
                  if ($response.StatusCode -eq 200) {
                      Write-Host "Health check passed"
                      $success = $true
                      break
                  } else {
                      Write-Host "Health check returned $($response.StatusCode), retrying..."
                  }
              } catch {
                  Write-Host "Waiting for server to start... ($($i+1)/$maxRetries)"
              }
              Start-Sleep -Seconds 1
          }

          if (-not $success) {
              Write-Error "Health check failed after $maxRetries attempts"
              exit 1
          }

      - name: Stop server
        shell: pwsh
        continue-on-error: true
        run: |
          Get-Process | Where-Object { $_.ProcessName -like "email-spam-detector*" } | Stop-Process -Force

  test-macos:
    name: Test macOS
    needs: build
    runs-on: macos-14

    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          path: ./artifacts

      - name: Locate macOS binary
        id: find
        run: |
          file=$(find ./artifacts -type f -name "email-spam-detector-*-macos-arm64" | head -n 1)
          if [ -z "$file" ]; then
            echo "macOS binary not found" >&2
            exit 1
          fi
          echo "exe_path=$file" >> $GITHUB_OUTPUT

      - name: Make executable
        run: chmod +x "${{ steps.find.outputs.exe_path }}"

      - name: Start server
        run: |
          "${{ steps.find.outputs.exe_path }}" --port 8000 &
          SERVER_PID=$!
          echo "SERVER_PID=$SERVER_PID" >> $GITHUB_ENV
          sleep 30

      - name: Test /api/v1/health
        run: |
          for i in {1..10}; do
            curl -sf "http://127.0.0.1:8000/api/v1/health" && exit 0
            echo "Waiting for server..."
            sleep 1
          done
          echo "Health check failed" >&2
          exit 1

      - name: Stop server
        if: always()
        run: |
          kill -9 $SERVER_PID || true

  test-linux:
    name: Test Linux
    needs: build
    runs-on: ubuntu-24.04

    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          path: ./artifacts

      - name: Locate Linux binary
        id: find
        run: |
          file=$(find ./artifacts -type f -name "email-spam-detector-*-linux-x86_64" | head -n 1)
          if [ -z "$file" ]; then
            echo "Linux binary not found" >&2
            exit 1
          fi
          echo "exe_path=$file" >> $GITHUB_OUTPUT

      - name: Make executable
        run: chmod +x "${{ steps.find.outputs.exe_path }}"

      - name: Start server
        run: |
          "${{ steps.find.outputs.exe_path }}" --port 8000 &
          SERVER_PID=$!
          echo "SERVER_PID=$SERVER_PID" >> $GITHUB_ENV
          sleep 30

      - name: Test /api/v1/health
        run: |
          for i in {1..10}; do
            curl -sf "http://127.0.0.1:8000/api/v1/health" && exit 0
            echo "Waiting for server..."
            sleep 1
          done
          echo "Health check failed" >&2
          exit 1

      - name: Stop server
        if: always()
        run: |
          kill -9 $SERVER_PID || true

  upload-release-assets:
    name: Upload Release Assets
    needs: [build, test-windows, test-macos, test-linux]
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository (required for gh CLI)
        uses: actions/checkout@v4

      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-files

      - name: Wait for release to exist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          for i in {1..12}; do
            gh release view "${GITHUB_REF_NAME}" && break
            echo "Waiting for release to be created..."
            sleep 10
          done

      - name: Upload executables to release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ls -R release-files
          gh release upload "${GITHUB_REF_NAME}" release-files/**/* --clobber
